# ParityType

**Namespace:** `SpawnDev.BlazorJS.JSObjects`  
**Source:** `JSObjects/Serial/ParityType.cs`  

> Serial port parity options https://wicg.github.io/serial/#paritytype-enum

## Values

| Name | JSON Value | Description |
|---|---|---|
| `None` | `"none"` | No parity bit is used. This is the default value. |
| `Even` | `"even"` | An even parity bit is used. This means that the number of 1 bits in the data must be even. |
| `Odd` | `"odd"` | An odd parity bit is used. This means that the number of 1 bits in the data must be odd. |

## Properties

| Property | Type | Access | Description |
|---|---|---|---|
| `ParityType` | `enum` | get | Serial port parity options https://wicg.github.io/serial/#paritytype-enum |

