# AudioSinkOptions

**Namespace:** `SpawnDev.BlazorJS.JSObjects`  
**Source:** `JSObjects/WebAudio/AudioSinkOptions.cs`  

> An object representing different options for a sink ID. Currently this takes a single property, type, with a value of "none". Setting this parameter causes the audio to be processed without being played through any audio output device. This is a useful option to minimize power consumption when you don't need playback along with processing.

## Properties

| Property | Type | Access | Description |
|---|---|---|---|
| `AudioSinkOptions` | `class` | get | An object representing different options for a sink ID. Currently this takes a single property, type, with a value of "none". Setting this parameter causes the audio to be processed without being played through any audio output device. This is a useful option to minimize power consumption when you don't need playback along with processing. |

